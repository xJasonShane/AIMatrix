{
  "name": "aimatrix",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "check:size": "node -e \"const fs=require('fs');const path=require('path');const zlib=require('zlib');const dir=path.join('dist','assets');if(!fs.existsSync(dir)){console.error('dist/assets not found - run build first');process.exit(1)}let js=0,css=0;for(const f of fs.readdirSync(dir)){const size=zlib.gzipSync(fs.readFileSync(path.join(dir,f))).length;if(f.endsWith('.js'))js+=size;else if(f.endsWith('.css'))css+=size}const fmt=b=>(b/1024).toFixed(1)+' kB';console.log('gzip  JS '+fmt(js)+'  CSS '+fmt(css));const budget=300*1024;if(js>budget){console.error('FAIL: JS gzip '+fmt(js)+' exceeds budget '+fmt(budget));process.exit(1)}console.log('OK: within '+fmt(budget)+' JS gzip budget')\"",
    "lint": "eslint src",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "framer-motion": "^11.3.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.18.4"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@tailwindcss/vite": "^4.3.3",
    "@testing-library/jest-dom": "^6.4.8",
    "@testing-library/react": "^16.0.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "eslint": "^10.11.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.7",
    "globals": "^17.12.0",
    "jsdom": "^24.1.1",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.5.4",
    "typescript-eslint": "^8.70.1",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
